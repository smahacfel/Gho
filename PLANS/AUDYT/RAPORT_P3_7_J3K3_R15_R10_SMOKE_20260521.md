# RAPORT P3.7-J3K3 R15-r10 Smoke

## Status

```text
R15-r10-j3k3 smoke: MINIMAL PASS
P3.7-J3K3 route/override propagation: RUNTIME VALIDATED for transport/entry
Lifecycle/on-chain labels: NOT VALIDATED
Small bounded collection: CONDITIONAL GO
Full collection / Phase B / P2 / live / threshold tuning: HOLD / NO-GO
```

## Run

```text
config = configs/rollout/shadow-burnin-v3-p37-counterfactual-probe-r15-smoke-r10-j3k3.toml
namespace = shadow-burnin-v3-p37-counterfactual-probe-r15-smoke-r10-j3k3
started = 2026-05-21T07:28:33Z
stopped = early after probe transport/entry stop gate
stop reason = probe_transport_rows=5 and probe_shadow_entry_rows=5 reached
```

The process was intentionally stopped after the smoke gate was reached instead
of waiting for the full timeout.

## V3 Replay

```text
v3_shadow_report_status = ok
deduped_v3_rows = 1
full_snapshot_payload_rows = 1
hash_only_rows = 0
stale_against_config = false

strict_replay_status = full_replay_ok
strict_replay_v3_rows = 1
strict_replay_bad_rows = 0
```

The low deduped V3 row count is expected because the run was stopped early after
the probe stop gate, not left to collect a broader decision sample.

## Probe Artifacts

```text
probe_selection_rows = 19
probe_skipped_rows = 15
probe_transport_rows = 5
probe_shadow_entry_rows = 5
probe_lifecycle_rows = 0
active_shadow_transport_rows = 0
active_shadow_entry_rows = 0
active_buy_rows = 0
```

Skip distribution:

```text
probe_rate_limit_exceeded = 10
max_probes_per_run_exceeded = 2
probe_concurrency_limit_exceeded = 1
verdict_type_not_in_sample_scope = 1
execution_account_not_ready = 1
```

The single execution-account skip was:

```text
execution_account_not_ready:creator_vault:6G2eB88pK6pu3J1dX3LXqv8K32CWjF4zwGMjsgtt3MZq
```

No `missing_bonding_curve` skip appeared in this smoke.

## Probe Transport / Entry

```text
probe_transport.execution_outcome = counterfactual_shadow_probe_simulated: 5
simulation_error_rows = 0
probe_entry_materialized = 5
transport_without_entry_rows = 0
buy_variant = legacy_buy: 5
token_param_role = token_amount: 5
```

## Join-Key Audit

```text
probe_readiness = ready_for_probe_transport_entry_join
probe_join_key_acceptance = pass
probe_join_quality = exact_probe_id_and_ab_record_id
probe_decision_join_acceptance = pass
probe_required_exact_decision_v3_join_coverage = 1.0
probe_chain_ab_record_id_coverage = 1.0
probe_chain_probe_id_coverage = 1.0
probe_transport_rows_with_ab_record_id = 5/5
probe_entry_rows_with_ab_record_id = 5/5
```

The audit still reports lifecycle/full-chain readiness as `not_ready` because
there are no lifecycle rows. That is expected for this short smoke and does not
invalidate the transport/entry result.

## Decision

J3K3 fixed the dominant route/override handoff blocker observed in Q6-r2:
the probe path can now produce real counterfactual transport and entry rows
from legacy buy candidates with exact V3 decision joins and no active BUY
mutation.

The next step is a small bounded collection, not a full collection:

```text
max_probe_dispatches_per_run = 25
max_concurrent = 1
runtime = bounded; stop on dispatch limit or hard failure
```

Stop immediately if any of these appear:

```text
feature/policy hash mismatch
probe entry materialization regression
simulation error spike
active BUY mutation
live/P2 path touched
JSONL corruption
```
