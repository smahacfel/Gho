# RAPORT P3.7-J3F R15-r5 Counterfactual Probe Smoke

Date: 2026-05-20

Status:

```text
R15-r5 smoke: NOT_READY_DIAGNOSED
V3/MFS replay path: PASS
Probe selection join: PASS
Probe transport / entry: ABSENT
Full / bounded collection: HOLD
Phase B / P2 / live / tuning: NO-GO
```

## Inputs

Config:

```text
configs/rollout/shadow-burnin-v3-p37-counterfactual-probe-r15-smoke-r5.toml
```

Namespace:

```text
shadow-burnin-v3-p37-counterfactual-probe-r15-smoke-r5
```

Runtime stdout:

```text
logs/shadow_run/shadow-burnin-v3-p37-counterfactual-probe-r15-smoke-r5/r15_r5_runtime_stdout.log
```

## Runtime Artifacts

Observed rows:

```text
decision coverage audit rows = 546
probe_selection.jsonl = 5
probe_skips.jsonl = 576
probe_transport.jsonl = missing / 0
probe_shadow_entries.jsonl = missing / 0
buys.jsonl = missing / 0
shadow_entries.jsonl = missing / 0
shadow_lifecycle.jsonl = missing / 0
```

No active shadow BUY/entry/lifecycle artifacts were generated by the probe
plane.

## V3 Replay

Post-run V3 reports:

```text
v3_rows = 169
full_snapshot_payload_rows = 169
hash_only_rows = 0
strict replay status = full_replay_ok
strict replay bad_rows = 0
stale_against_config = false
```

This confirms that the R15-r5 decision-side V3/MFS replay path remained clean.

## Probe Selection

Probe selection rows:

```text
probe_selection_rows = 5
probe_selection rows with ab_record_id = 5
probe_selection rows with probe_id = 5
probe_selection exact decision/V3 join = 5/5
feature_hash_mismatch = 0
policy_hash_mismatch = 0
```

Selected probe buckets:

```text
v3_reject_manipulation_contradiction = 4
active_reject_v3_pending = 1
```

## Probe Skips

All selected probes were stopped before probe transport/entry by explicit
required-account precheck failures.

Skip reason counts across the full run:

```text
probe_rate_limit_exceeded = 350
max_probes_per_run_exceeded = 172
verdict_type_not_in_sample_scope = 48
probe_execution_precheck_failed = 5
probe_concurrency_limit_exceeded = 1
```

Precheck failures for the five selected probes:

```text
missing_required_account:bonding_curve_v2 = 4
missing_required_account:creator_vault = 1
```

J3F-specific check:

```text
missing_required_account:payer_pubkey = 0
missing_required_account:user_volume_accumulator = 0
missing_required_account:transaction_account:<known user volume PDA> = 0
```

## Join-Key Audit

Post-run join-key audit:

```text
probe_readiness = not_ready
probe_join_key_acceptance = fail
probe_required_exact_decision_v3_join_coverage = 1.0
probe_selection exact decision/V3 join = 5/5
probe_transport_rows = 0
probe_entry_rows = 0
```

The audit correctly remains not ready because exact selection join is not enough
for runtime probe readiness. Transport and entry rows are required for a smoke
PASS.

## Decision

R15-r5 is useful but not a PASS.

It confirms:

- J3F fixed the previous generic `transaction_account` / user-volume blocker.
- V3 replay is clean.
- Selection metadata and source hashes remain continuous.
- Probe rows did not mutate active BUY artifacts.

It also blocks collection because:

- no probe transport rows were generated;
- no probe entry rows were generated;
- all five selected probes stopped on strict routed execution accounts.

Next gate:

```text
P3.7-J3G Probe Strict Execution Account Readiness
```

J3G must preserve strict precheck for true execution accounts. It should
determine whether missing `bonding_curve_v2` and `creator_vault` indicate:

- sampled rows are not execution-ready at decision time,
- the probe should wait within a decision-time-safe readiness window,
- account override/build path is incomplete,
- or sampling eligibility must be restricted to execution-ready rows.

No collection, Phase B, P2, live, active policy, IWIM, or threshold change is
authorized by this smoke.
