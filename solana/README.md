# Solana local wallet area

Ten katalog nie może zawierać trackowanych keypairów rolloutowych.

- Trzymaj wallety lokalnie poza repo albo w ignorowanym katalogu `wallets/`.
- `solana/*.json` jest ignorowane przez git.
- Jeżeli potrzebujesz lokalnego keypaira do testów, utwórz go poza tracked state repo.
